# Decision-buffer learnings

One dated line per entry, in Jet's own words, plus the buffer path — appended
whenever Jet flags a buffer as unhelpful in-session. See `SKILL.md` §10.

<!-- 2026-09-09: "this buffer isn't helping" — logs/decisions/2026-09-09-foo.md -->
- 2026-09-14 — Jet: "The format of the /parked items run is very confusing" — SPECIFIC to the /parked-items review buffer (~/.claude/parked-items/review-2026-09-14.md: 31 open items, 21 carried forward, 8-way action row per item), not decision buffers in general (the same day's interview, triage and code-review-triage buffers drew no such complaint). Asked for a companion HTML with the full ledger notes + action definitions. Fix belongs in the parked-items skill's Step-3 format, not in this skill's shape rules.
- 2026-09-14 — Jet, on the day's third big buffer (audit of 100+ pieces): "using the nvim buffer for this feels odd, I think we need a better flow (one that uses an html page and sensible defaults -> but we need a way to ask questions here and possibly tie items together)." GENERAL signal, unlike the parked-items note above: nvim buffers are right for choice/interview/short triage, wrong for 50+-row reviews. Direction: an HTML review page (pre-selected defaults, per-row ask/note, group tag to tie rows, filters, Submit → answers.json + tmux wait-for signal) as a seventh shape; B1's weekly review inherits it.
- 2026-09-14 — review-page v1 first run (piece audit, 157 rows): Jet reviewed everything but 80 rows read as untouched because agreeing with the pre-selected default required no click. Jet: "I thought I did them all." Fix shipped same day: per-row Agree ✓ (a / A), live untouched badge + submit confirm, spec flag accept_defaults_default. Rule: a default the user agrees with must be one explicit click, never inferred from silence — but silence must also be impossible to miss.
- 2026-09-14 — CONTRACT CHANGE (Jet): in the review-page flow, no action on a row = accept its suggestion; opt-out is a changed verdict or a note (row note or global questions). "I think we can assume in this flow that no action means okay unless I add a note." Supersedes the same-day Agree-button lesson's framing: Agree stays as a nicety, but silence is consent here — the opposite of the nvim-buffer rule, and that difference must be stated in the page intro every time.
